# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtualenv
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Flask Instance folder (if you don't want to commit your local dev DB)
# instance/
# However, if you were using SQLite and wanted to deploy it, you might commit this.
# For PostgreSQL, this folder is usually not committed unless it has other configs.

# IDE / OS specific
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs directory (if you created one at the root and don't want to commit logs)
# logs/

# Environment files
.env
*.env
.env.*

# Large data files (if not using LFS for them, or if they are truly temporary)
# data/*_backup.json
# data/large_dataset_not_for_git.csv